<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Pergunta Detalhada</title>
    <style>
        /* Reset básico */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* Estilos para navbar e container principal */
        .container {
            flex-grow: 1; /* Faz o conteúdo crescer para ocupar o restante da tela */
            width: 100%;
            max-width: 800px;
            margin: 20px auto;
            background: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 1.5rem;
        }

        .pergunta-titulo {
            font-size: 1.8rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 0.5rem;
            border-bottom: 1px solid #cccccc;
            padding-bottom: 0.5rem;
        }

        .descricao {
            color: #666;
            font-size: 1.1rem;
            line-height: 1.6;
            margin-bottom: 2rem;
        }

        /* Estilo para respostas */
        .respostas-section {
            margin-top: 1rem;
        }

        .respostas-title {
            font-size: 1.5rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 1rem;
        }

        .resposta {
            background-color: #f4f4f4;
            padding: 1rem;
            border-radius: 6px;
            margin-bottom: 1rem;
            color: #444;
            line-height: 1.5;
        }

        /* Link para voltar */
        .back-link {
            display: block;
            margin-top: 1rem;
            text-align: right;
            color: #3498db;
            text-decoration: none;
            font-weight: bold;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        /* Estilo para o formulário */
        form {
            margin-top: 2rem;
            background-color: #ffffff;
            padding: 1.5rem;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        label {
            display: block;
            font-weight: bold;
            color: #333;
            margin-bottom: 0.5rem;
        }

        textarea {
            width: 100%;
            padding: 1rem;
            font-size: 1rem;
            border: 1px solid #cccccc;
            border-radius: 4px;
            resize: vertical;
            min-height: 150px;
            margin-bottom: 1rem;
            color: #333;
        }

        button {
            padding: 1rem 2rem;
            background-color: #4CAF50;
            color: #ffffff;
            border: none;
            border-radius: 4px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #45a049;
        }

        .form-container {
            margin-top: 2rem;
            padding: 2rem;
            border-radius: 8px;
            background-color: #ffffff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 800px;
            margin-bottom: 2rem;
        }

        form input[type="hidden"] {
            display: none;
        }

        /* Estilo para o footer fixo */
        footer {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 1rem 0;
            position: relative;
            margin-top: auto; /* Faz com que o footer fique sempre na parte inferior */
            width: 100%;
        }
    </style>
</head>
<body>

    <!-- Partial para Navbar -->
    <%- include('partials/navbar') %>

    <div class="container">
        <!-- Exibir Pergunta -->
        <h1 class="pergunta-titulo"><%= pergunta.pergunta %></h1>
        <p class="descricao"><%= pergunta.descricao %></p>

        <form action="/responder" method="post">
            <label for="corpo">Resposta</label>
            <textarea name="corpo" placeholder="Digite sua resposta aqui"></textarea>
            <input type="hidden" name="pergunta" value="<%= pergunta.id %>">
            <button type="submit">Responder</button>
        </form>

        <!-- Exibir Respostas -->
        <div class="respostas-section">
            <h2 class="respostas-title">Respostas</h2>

            <!-- Exemplo de respostas estáticas (substitua com código dinâmico quando implementar) -->
            <% respostas.forEach(resposta =>{ %>
                <div class="resposta">
                    <p><%= resposta.corpo %></p>
                </div>
            <% }) %>
        </div>

        <!-- Link para voltar -->
        <a href="/" class="back-link">Voltar para a lista de perguntas</a>
    </div>

    <!-- Partial para Footer -->
    <%- include('partials/footer') %>

</body>
</html>
